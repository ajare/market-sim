/**
 * Weather profile names, moved into @market-sim/shared (shared with the
 * simulation engine, see src/sim/weather.ts's WEATHER_PROFILES) -- this
 * re-export keeps every existing `from "./weatherProfiles"` import in the
 * editor working unchanged.
 */
export * from "@market-sim/shared/weatherProfiles";
