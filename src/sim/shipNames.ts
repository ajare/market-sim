/**
 * Ship-name generators, moved into @market-sim/shared (shared with the
 * editor) -- this re-export keeps every existing `from "./shipNames"` import
 * in the sim working unchanged. See @market-sim/shared/src/shipNames.ts for
 * the actual pools/generator.
 */
export * from "@market-sim/shared/shipNames";
