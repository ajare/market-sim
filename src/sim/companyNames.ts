/**
 * Company-name generators, moved into @market-sim/shared (shared with the
 * editor) -- this re-export keeps every existing `from "./companyNames"`
 * import in the sim working unchanged. See
 * @market-sim/shared/src/companyNames.ts for the actual pools/generator.
 */
export * from "@market-sim/shared/companyNames";
