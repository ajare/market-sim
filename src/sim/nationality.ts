/**
 * Nationality types/pools, moved into @market-sim/shared (shared with the
 * editor) -- this re-export keeps every existing `from "./nationality"`
 * import in the sim working unchanged. See
 * @market-sim/shared/src/nationality.ts for the actual types/pools/generator.
 */
export * from "@market-sim/shared/nationality";
