/**
 * Captain-name generators, moved into @market-sim/shared (shared with the
 * editor) -- this re-export keeps every existing `from "./names"` import in
 * the sim working unchanged. See @market-sim/shared/src/names.ts for the
 * actual pools/generator.
 */
export * from "@market-sim/shared/names";
