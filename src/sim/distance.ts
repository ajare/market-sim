/**
 * Distance model, moved into @market-sim/shared (shared with the editor) --
 * this re-export keeps every existing `from "./distance"` import in the sim
 * working unchanged. See @market-sim/shared/src/distance.ts for the actual
 * implementation.
 */
export * from "@market-sim/shared/distance";
